<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>movies</title>
</head>
<body>
    <h1> peliculas recomenadas </h1>
    <ul>
        <% moviesAll.forEach(movie => { %>
            <li>
                <a href="/movies/details/<%= movie.id %>"><%= movie.title %> </a>
            </li> 
        <% }) %>
    </ul>
</body>
</html>